1. 修复services/type.d.ts中重复定义的类型（QuestionTypeItem、QuestionData、QuestionType）
2. 为Operation.tsx中的Form.useForm()添加明确的类型参数
3. 为formatMenuList函数添加明确的返回值类型
4. 优化handleOptionChange函数的参数类型
5. 为options状态添加明确的类型定义
6. 为form.validateFields()添加类型参数