1. **修改HTTP方法**：将request.post改为request.get
2. **调整参数传递方式**：axios GET方法的第二个参数是配置对象，需要通过params属性传递查询参数
3. **具体修改点**：将`request.post<BaseResponse<TestList>>('/exam/list', params)`改为`request.get<BaseResponse<TestList>>('/exam/list', { params })`
4. **验证修复结果**：确保修改后代码符合axios API规范，能够正确发送GET请求