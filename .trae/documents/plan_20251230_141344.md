1. **添加状态存储完整的创建人和科目信息**：
   - 添加`allCreators`状态，存储所有唯一的创建人
   - 添加`allSubjects`状态，存储所有唯一的科目

2. **在组件初始化时获取完整数据**：
   - 添加useEffect，在组件加载时调用API获取所有试卷数据
   - 仅获取用于生成选项所需的字段，优化性能
   - 将获取到的数据存储到本地状态

3. **生成完整的选项列表**：
   - 从完整数据中提取唯一的创建人列表
   - 从完整数据中提取唯一的科目列表
   - 确保选项列表不随分页变化而变化

4. **优化API调用**：
   - 使用专门的API调用获取所有试卷数据，或者在初始加载时设置较大的pagesize
   - 考虑添加缓存机制，避免重复调用

5. **修复数据一致性**：
   - 确保创建人和科目选项与实际数据保持一致
   - 处理API调用失败的情况

6. **优化代码结构**：
   - 保持代码简洁，避免冗余逻辑
   - 确保数据流向清晰