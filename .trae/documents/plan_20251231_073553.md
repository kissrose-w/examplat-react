1. **修复创建人数据不更新的问题**：
   - 在搜索时更新`allTestPapers`，确保获取最新的创建人数据
   - 增加`pagesize`的值，确保能获取所有试卷
   - 在`getList`调用成功后更新`allTestPapers`

2. **优化数据获取逻辑**：
   - 简化`allTestPapers`的获取逻辑
   - 确保`allTestPapers`始终包含最新的创建人数据
   - 提高代码的可读性和可维护性

3. **测试和验证**：
   - 确保修复后搜索创建人数据能够正确更新
   - 确保在有新试卷创建或已有试卷修改时，创建人数据能自动更新
   - 确保代码符合TypeScript类型要求