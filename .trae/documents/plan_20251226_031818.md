1. **修改columns.tsx**：
   - 更新ColumnsProps接口，添加list属性
   - 修改columns函数，接受list参数
2. **修改PaperBank.tsx**：
   - 在调用columns函数时传入list参数
3. **确保类型安全**：
   - 为list参数添加正确的类型注解
   - 确保list在columns中的使用方式符合类型要求
4. **验证修改结果**：
   - 运行构建命令确保类型正确，功能正常