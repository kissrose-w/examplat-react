1. **修改columns组件**：
   - 更新Props类型，添加classifyList参数
   - 修改科目类型列的render函数，根据_id查找对应的名称
   - 添加空值处理，确保在没有找到对应名称时显示合理的默认值

2. **修改PaperBank组件**：
   - 添加获取科目列表的逻辑
   - 调用getClassifyList API获取所有科目
   - 将获取到的科目列表传递给columns组件

3. **确保数据一致性**：
   - 确保在科目列表加载完成后再渲染表格
   - 处理API调用失败的情况
   - 确保渲染时使用最新的科目列表

4. **测试和验证**：
   - 确保科目类型列正确显示对应名称
   - 确保在没有找到对应名称时显示合理的默认值
   - 确保代码符合TypeScript类型要求