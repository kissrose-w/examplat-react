我已经查看了CreatePaper.tsx文件，发现"去第二步"的按钮已经被放到了右边，使用了flex布局和justifyContent: 'flex-end'。但是，我注意到在第二步和第三步中，按钮是左对齐的，没有使用flex布局。为了保持UI的一致性，我建议调整所有步骤的按钮布局，确保它们都使用flex布局，并且"去第三步"和"提交"按钮也靠右对齐。

具体修改方案：
1. 对于第二步（step === 1），将按钮包装在一个div中，使用flex布局和justifyContent: 'flex-end'，并添加适当的间距。
2. 对于第三步（step === 2），同样将按钮包装在一个div中，使用flex布局和justifyContent: 'flex-end'，并添加适当的间距。
3. 确保所有步骤的按钮布局保持一致，提升用户体验。

这样修改后，所有步骤的主要操作按钮（"去第二步"、"去第三步"、"提交"）都会靠右对齐，保持UI的一致性和美观性。