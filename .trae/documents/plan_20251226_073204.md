1. **更新TestPaper store**：
   - 修改State接口，添加params字段
   - 更新getList方法，使其接受params参数
   - 使用传入的params获取数据，并存储到store中

2. **修改PaperBank组件**：
   - 导入testListInfo store
   - 移除本地的list、loading、total状态，使用store中的数据
   - 保留本地params状态，用于分页控制
   - 修改useEffect，调用store的getList方法并传递params
   - 确保分页切换时，调用store的getList方法更新数据
   - 保留现有的onDel功能，但删除后调用store的getList刷新数据

3. **确保数据绑定**：
   - PaperBank的list、loading、total与store中的数据绑定
   - 分页切换时同时更新store中的数据
   - 保留PaperBank现有的所有功能

4. **验证修复结果**：
   - 运行构建命令确保语法正确
   - 检查功能是否正常工作

通过以上修改，PaperBank组件将使用store中的数据，同时保留现有的功能，实现数据绑定。