1. **修改columns.tsx**：将columns从常量改为函数，接受props参数（包含onDel方法）
2. **修改PaperBank.tsx**：在渲染Table组件时，调用columns函数并传递onDel方法
3. **具体修改点**：
   - 在columns.tsx中，将`export const columns: TableColumnsType<TestListItem> = [`改为`export const columns = ({ onDel }: { onDel: (id: string) => void }) => [`
   - 在columns.tsx的删除按钮render函数中，添加点击事件处理：`onClick={() => onDel(record._id)}`
   - 在PaperBank.tsx中，将`columns={columns}`改为`columns={columns({ onDel })}`
4. **确保类型安全**：为columns函数添加正确的类型注解
5. **验证修复结果**：运行构建命令确保类型正确，功能正常