1. **修复options生成逻辑**：
   - 将嵌套的map函数改为使用find方法查找匹配的科目
   - 确保map函数有返回值
   - 返回正确的label值，使用科目名称而不是ID
   - 处理未找到匹配科目的情况

2. **优化代码结构**：
   - 简化options生成逻辑
   - 提高代码可读性和性能
   - 确保类型安全

3. **测试和验证**：
   - 确保修复后查询科目下拉选择框正确显示科目名称
   - 确保在没有找到匹配科目时显示合理的默认值
   - 确保代码符合TypeScript类型要求