1. **导入TableColumnsType类型**：从antd的Table组件中导入TableColumnsType类型
2. **为columns添加类型注解**：将columns的类型定义为`TableColumnsType<TestListItem>`
3. **确保类型一致**：确保columns的类型与Table组件的dataSource类型(TestListItem[])匹配
4. **具体修改点**：
   - 在导入语句中添加`TableColumnsType`
   - 将`const columns = [`改为`const columns: TableColumnsType<TestListItem> = [`
5. **验证修复结果**：运行构建命令确保类型正确