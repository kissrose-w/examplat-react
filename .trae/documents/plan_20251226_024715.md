1. **创建单独的columns文件**：在paperBank目录下创建一个新的columns.ts文件
2. **定义并导出columns**：将现有的columns定义移动到新文件中，并添加正确的类型注解
3. **导入必要依赖**：在新文件中导入所需的类型和组件
4. **修改PaperBank.tsx**：删除原有的columns定义，导入新文件中的columns
5. **确保类型一致**：确保columns的类型与Table组件的dataSource类型匹配
6. **验证修改结果**：运行构建命令确保类型正确，功能正常