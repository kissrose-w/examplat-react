1. **分析报错原因**：
   - `getTestPaperList` 函数使用了 `request.get` 方法，但传递了 `params` 作为第二个参数，这不符合 axios API 规范
   - axios.get 方法的第二个参数是配置对象，应该通过 `params` 属性传递查询参数，而不是直接传递数据
   - 从之前的代码上下文看，该接口应该使用 POST 方法，因为查询试卷列表可能需要发送数据

2. **具体修复方案**：
   - 将 `request.get` 改为 `request.post`，因为查询试卷列表通常需要发送数据
   - 或者将 params 作为配置对象的 `params` 属性传递，保持使用 GET 方法
   - 根据 API 设计原则和前后端约定，选择合适的 HTTP 方法

3. **验证修复结果**：
   - 修改后运行构建命令验证
   - 确保与前端调用代码兼容