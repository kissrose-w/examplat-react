1. **修改菜单展开逻辑**：移除 `openKeys` 的自动计算逻辑，使点击学生管理时不会自动展开子菜单
2. **完善类型定义**：确保 `selectedKeys` 和 `openKeys` 的类型与 Ant Design Menu 组件的类型要求一致
3. **具体修改点**：
   - 将第45行的 `openKeys` 计算逻辑修改为固定空数组或受控逻辑
   - 确保 `selectedKeys` 和 `openKeys` 的类型定义明确，与 MenuProps 中的类型一致
   - 可考虑将 `openKeys` 设为 useState 管理的受控组件，或移除自动展开逻辑
4. **预期效果**：点击学生管理菜单项时，只会导航到对应页面，不会展开子菜单，同时类型定义更加准确