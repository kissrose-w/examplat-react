1. **修改CreatePaper.tsx**：
   - 导入useNavigate钩子
   - 添加创建试卷按钮
   - 实现点击按钮时导航到试卷库页面

2. **确保菜单高亮**：
   - 菜单组件会根据当前路径自动高亮对应的菜单项
   - 导航到试卷库路径后，菜单中的试卷库选项会自动高亮

3. **具体修改点**：
   - 在CreatePaper.tsx中导入useNavigate
   - 添加Button组件和onClick事件处理
   - 点击事件中调用navigate('/paper/paper-bank')

4. **验证修复结果**：
   - 确保点击创建试卷按钮后能正确导航到试卷库页面
   - 确保菜单中的试卷库选项被正确高亮